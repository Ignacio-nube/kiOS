{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "rewrites": [
    {
      "//": "`/gracias` es el back_url de Mercado Pago. Como la landing es un SPA de un solo index.html, sin este rewrite ese link daría 404 justo después de que el cliente pagó.",
      "source": "/gracias",
      "destination": "/index.html"
    }
  ],
  "headers": [
    {
      "//": "Los assets con hash en el nombre son inmutables por definición; el HTML nunca, o un deploy nuevo no lo vería nadie.",
      "source": "/assets/(.*)",
      "headers": [{ "key": "cache-control", "value": "public, max-age=31536000, immutable" }]
    },
    {
      "source": "/(.*)",
      "headers": [
        { "key": "x-content-type-options", "value": "nosniff" },
        { "key": "referrer-policy", "value": "strict-origin-when-cross-origin" },
        { "key": "x-frame-options", "value": "DENY" }
      ]
    }
  ]
}
